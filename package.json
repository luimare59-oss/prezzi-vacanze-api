{
  "name": "holiday-pricing-api-netlify",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "netlify-cli": "^17.31.1"
  },
  "scripts": {
    "dev": "netlify dev",
    "serve": "netlify dev",
    "build": "echo 'No build step'",
    "deploy": "netlify deploy"
  }
}
